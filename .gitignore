/.config/fish/fish_variables

# the first `/` means start it the root of the git directory
# the last `/` means the thing we are ignoring is a folder
/.config/fish/functions/
/.config/fish/completions/
/.config/fish/conf.d/

# this is how you would allow (ie not ignore) certain functions
# useful if you write your own
# !/fish/.config/fish/functions/my_special_function.fish

Session.vim
